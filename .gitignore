# Credentials. Never tracked; see the note at the top of README.md.
.env*
*.key
.credentials*

# Raw agent transcripts: ~1.4 GB across ~9.9k files.
# Graded results/ and agent workspace artifacts under ws/ are tracked.
transcript.jsonl

# Local spec checker: measures the built figures and manuscript against the
# Nature figure/formatting guides. A working tool, not part of the paper.
paper_plot_script/check_nature_specs.py

# ICLR 2027 style files, kept locally as the formatting reference. Upstream
# template, not our work.
iclr-2027-style-files.zip
iclr2027/

# TUI harness: per-prompt agent workspaces (artifacts + transcripts)
tui_workspaces/
# TUI agent runs: <cwd>/Workspace/<md5(prompt)>/ artifacts (README §0)
Workspace/
# ...except the small tracked in-TUI evidence transcripts (README §0).
!tui/evidence/tui_evidence_harness/transcript.jsonl
!tui/evidence/tui_evidence_live156/transcript.jsonl

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.ipynb_checkpoints/

# LaTeX build artifacts
*.aux
*.log
# ...except the small TUI e2e run logs (README §0).
!tui/evidence/*.log
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
*.bbl
*.blg

# OS
.DS_Store

# Agents
CLAUDE.md
AGENTS.md